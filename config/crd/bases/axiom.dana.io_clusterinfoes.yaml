---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.2
  name: clusterinfoes.axiom.dana.io
spec:
  group: axiom.dana.io
  names:
    kind: ClusterInfo
    listKind: ClusterInfoList
    plural: clusterinfoes
    singular: clusterinfo
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ClusterInfo is the Schema for the clusterinfoes API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ClusterInfoSpec defines the desired state of ClusterInfo.
            type: object
          status:
            properties:
              gpus:
                items:
                  description: GPUInfo describes GPU resources on a node
                  properties:
                    count:
                      type: integer
                    model:
                      type: string
                    nodeName:
                      type: string
                  type: object
                type: array
              kubernetesVersion:
                type: string
              lastUpdated:
                format: date-time
                type: string
              nodeCount:
                type: integer
              nodes:
                items:
                  description: NodeInfo holds information about a node
                  properties:
                    allocatable:
                      description: NodeCapacity describes resource capacity or allocatable
                      properties:
                        cpu:
                          type: string
                        memory:
                          type: string
                        pods:
                          type: string
                        storage:
                          type: string
                      type: object
                    capacity:
                      description: NodeCapacity describes resource capacity or allocatable
                      properties:
                        cpu:
                          type: string
                        memory:
                          type: string
                        pods:
                          type: string
                        storage:
                          type: string
                      type: object
                    externalIP:
                      type: string
                    internalIP:
                      type: string
                    kubeletVersion:
                      type: string
                    name:
                      type: string
                    osImage:
                      type: string
                  type: object
                type: array
              podCount:
                type: integer
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
